#  Can you change the values inside a list which is contained in set S?

s = {8, 7, 12, "Insha", [1,2]}

# list cannot be included in set s
# sets are unindexed (so set not use as index)
# sets are immutable and hashable